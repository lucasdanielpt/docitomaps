{
  "name": "docitomapas",
  "version": "1.0.0",
  "private": true,
  "description": "Guia 3D de Roteiros de Viagem",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "pnpm -r --parallel --stream run dev",
    "dev:web": "pnpm --filter @docitomapas/web run dev",
    "dev:api": "pnpm --filter @docitomapas/api run dev",
    "build": "pnpm -r run build",
    "build:vercel": "pnpm --filter @docitomapas/web build",
    "vercel-build": "pnpm --filter @docitomapas/web build && rm -rf dist && cp -r apps/web/dist dist",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\""
  },
  "devDependencies": {
    "@vercel/node": "^5.1.8",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
